# Tareas: HU-003

- [ ] 1. Crear directorios: `controllers`, `middlewares`, `models`, `routes`, `services`, `utils` en `/backend`.
- [ ] 2. Crear middleware `middlewares/errorHandler.js` para capturar errores globales.
- [ ] 3. Crear ruta `routes/health.js` con el endpoint GET `/api/health`.
- [ ] 4. Crear archivo `routes/index.js` para centralizar las rutas.
- [ ] 5. Actualizar `server.js` para usar las rutas y el middleware de errores.
- [ ] 6. Probar en Postman/Navegador que `/api/health` funciona.